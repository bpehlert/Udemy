// An arrow function changes traditional code as follows:

let input = 2;

function functionName(input) {
  let output = input*2;
  return output;
};

const arrowName = (input) => {
  return input*2;
};

// OR

const arrowName = input => input*2

// You don't need the ()
